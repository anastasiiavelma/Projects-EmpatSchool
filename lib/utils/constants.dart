import 'package:flutter/material.dart';

// Text size
const largeTextSize = 25.0;
const mediumTextSize = 18.0;
const smallTextSize = 13.0;
const iconSize = 25.0;

// Colors
Color kBackgroundColor = Colors.black;
Color kPrimaryColor = Colors.white;
Color itemColor = Colors.grey;
Color kAccentColor = Colors.blue;

// Border for highlighters
Color borderColor1 = Colors.pink;
Color borderColor2 = Colors.red;
Color borderColor3 = Colors.yellow;

// Sized Box
const smallSizedBoxSizeHeight = SizedBox(height: 4);
const smallSizedBoxSizeWidth = SizedBox(width: 4);

const smallMediumSizedBoxSizeHeight = SizedBox(height: 6);
const smallMediumSizedBoxSizeWidth = SizedBox(width: 6);

const mediumSizedBoxSizeHeight = SizedBox(height: 10);
const mediumSizedBoxSizeWidth = SizedBox(width: 8);

const largeSizedBoxSizeHeight = SizedBox(height: 20);

// padding
const smallPadding = EdgeInsets.all(3.0);
const mediumPadding = EdgeInsets.all(10.0);
const largePadding = EdgeInsets.all(15.0);

// spacer
const spacer = Spacer();

// Divider
const divider = Divider(
  thickness: 0.2,
);
